# Database
DATABASE_URL=postgres://postgres:password@127.0.0.1:5432/swajyot
PAYLOAD_SECRET=your-long-random-secret-here

# Public site URL — used by Payload for auth cookies, CSRF, and media URLs.
# For production behind a reverse proxy, set SERVER_URL at *runtime* (not only at build):
SERVER_URL=http://test.swajyot.co.in
NEXT_PUBLIC_SERVER_URL=http://test.swajyot.co.in

# Mail (optional)
# MAIL_HOST=
# MAIL_PORT=587
# MAIL_USER=
# MAIL_PASS=
# MAIL_FROM=
# MAIL_FROM_NAME=Swajyot
